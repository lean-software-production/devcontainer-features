// What the student is told after Tutor opens a side chat. A plugin cannot
// select another plugin's panel tab, so the side chat's tab arrives in the
// coach thread's right panel unselected, and the hint points to it.
export const SIDE_CHAT_HINT =
  "Your side chat is in the “Side chat” tab of the coach thread's right panel. Press Ctrl+J if the panel is hidden.";
